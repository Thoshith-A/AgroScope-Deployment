// Lightweight placeholder for the 3D scene to keep
// the Forecast page stable across all environments.
// It preserves the API shape without pulling in WebGL.

export function ForecastChartScene(_: { data: number[] }) {
  return null;
}


